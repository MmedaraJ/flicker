sub init()
    m.spinAnimation = m.top.findNode("spinAnimation")
    m.top.observeField("visible", "onVisibleChange")
end sub

sub onVisibleChange()
    if m.top.visible
        m.spinAnimation.control = "start"
    else
        m.spinAnimation.control = "stop"
    end if
end sub
